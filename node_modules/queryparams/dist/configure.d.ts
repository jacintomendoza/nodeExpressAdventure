import { Options } from "./types";
export declare const configure: <T>(defaults: T, queryString?: string | undefined) => {
    defaults: T;
    query: Record<string, unknown>;
    params: T;
    schema: {
        param: string;
        description?: string | undefined;
        type: string;
        default: any;
    }[];
    encode: (nextParams: Options) => string;
    reconfigure: (nextParams: Options) => void;
};
export default configure;
